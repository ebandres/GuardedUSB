|[
  declare
    x, y, z, p : int, int, array[0..2], int

  x := 1;
  y := 1;
  z := 4, 3, 2;
  if x==y -->
    x := z[2]+1
  [] x != y -->
    |[
      declare
        a : int;
        q : array[2..2]
      a := 3;
      q := atoi(q);
      println a
    ]|
  fi
]|
