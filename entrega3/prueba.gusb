|[
  declare
    x, y, z, p : int, int, array[0..2], bool;
    a, b, c, d : int

  x := 1;
  y := 3 + 2;
  p := true;
  z := 4, 3, 2;
  a := z[2];
  b := a;

  for i in 2 to 10 -->
    |[
      //i := 2;
      println i || " "
    ]|
  rof;

  if x == y -->
    x := 1 + 2
  [] x != y -->
    |[
      declare
        z : int;
        q : array[-1..3]
      z := x;
      z := atoi(q);
      println z
    ]|
  fi
  //a := 2
]|
