|[
  declare
    x, y, z, p : int, int, array[0..2], bool;
    a, b, c, d : int

  x := 1;
  y := 1;
  z := 4, 3, 2;
  if x==y -->
    x := z[2]+1
  [] x != y -->
    |[
      declare
        z : int;
        q : array[-1..3]
      z := 3;
      q := atoi(q);
      println z
    ]|
  fi
  //a := 2
]|
