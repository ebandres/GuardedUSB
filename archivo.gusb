|[
  declare
    a, b, c, g, e: int;
    d, e, f : array[0..2];
//	ba : int;
//	a : int;
//	d : int;
//    b := 1;
//    a := b + 2 + 5;
    e := max(d);
    a := min(d);
    c := size(d);
    g := atoi(a);
//    read e
    // Esto es un comentario. Debe ser ignorado.
]|
