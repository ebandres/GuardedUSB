|[
  declare
    a, b, c, g, j: int;
    d, e, f, x, y : array[0..2];
//	ba : int;
//	a : int;
//	d : int;
    b := 1;
    a := b + 2 + 5;
    e := max(d);
    a := min(d);
//    c := size(d);
//    g := atoi(a);
    read x;
    println "Variable \"i\" es igual a: " || i;
    // Esto es un comentario. Debe ser ignorado.
]|
